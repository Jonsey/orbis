<%= my_page_title "Edit account details" %>

<% formatted_form_for [:admin, @user] do |f| %>
<%= f.error_messages %>

<%= render :partial => "user_fields", :locals => { :f => f } %>
<div>
  <%= f.submit "Update" %>
</div>
<% end %>

