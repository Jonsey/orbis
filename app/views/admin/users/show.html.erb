<%= my_page_title "User details" %>
<div class="show-user">
  <fieldset>
   <div class="row"> Name: <%=@user.to_s%></div>
  <div class="row"> Email: <%=mail_to @user.try(:email)%></div>
  <div class="row"> Telephone: <%=@user.try(:telephone)%></div>
 </fieldset>
</div>
